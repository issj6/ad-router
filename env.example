# 数据库配置（必需）
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_USER=your_db_user
MYSQL_PASSWORD=your_db_password
MYSQL_DB=ad_router

# CORS配置（可选）
# ALLOWED_ORIGINS=http://localhost:3000,https://yourdomain.com

# 使用说明：
# 1. 复制此文件为 .env
# 2. 修改为实际的数据库配置
# 3. 在启动应用前加载环境变量：
#    - 开发环境：source .env
#    - Docker：使用 --env-file .env
#    - Docker Compose：自动读取 .env 文件
